package com.qp.groceryapi;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class GroceryApiApplication {

	public static void main(String[] args) {
		SpringApplication.run(GroceryApiApplication.class, args);
	}
	// PostMan Api collection is already available to test Api endpoints with file name Api_endpoint.postman_collection
}
